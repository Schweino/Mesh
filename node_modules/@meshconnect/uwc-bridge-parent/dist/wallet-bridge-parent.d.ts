interface EIP6963ProviderInfo {
    uuid: string;
    name: string;
    icon: string;
    rdns: string;
}
export interface EIP6963WalletProxy {
    uuid: string;
    info: EIP6963ProviderInfo;
    provider: Record<string, any>;
}
export interface WalletStandardProxy {
    name: string;
    icon: string;
    chains: string[];
    features: string[];
    wallet: Record<string, any>;
    adapter: Record<string, any>;
}
export interface BridgeAPI {
    getEIP6963Wallets(): Promise<EIP6963WalletProxy[]>;
    getWalletStandardWallets(): Promise<WalletStandardProxy[]>;
    refreshWallets(): Promise<void>;
}
/**
 * WalletBridgeParent - Manages wallet detection in parent window and exposes to iframe
 */
export declare class WalletBridgeParent {
    private bridge;
    /**
     * Initialize bridge connection to iframe
     */
    initialize(iframe: HTMLIFrameElement): Promise<void>;
    private waitForIframe;
    /**
     * Refresh wallet detection
     */
    refreshWallets(): Promise<void>;
}
export {};
//# sourceMappingURL=wallet-bridge-parent.d.ts.map